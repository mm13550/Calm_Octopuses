# Tools

This folder is for optional local utilities that are not part of the primary Streamlit product surface or automated test suite.

- `diagnostics_app.py`: legacy diagnostic Streamlit app for inspecting embeddings, training logs, and model experiments.

Run a tool directly when needed, for example:

```powershell
streamlit run tools/diagnostics_app.py
```
