---
description: Push all changes to github with a description
---

// turbo-all
1. Add all changes to the staging area:
```bash
git add .
```
2. Commit the changes with a descriptive message (replace with the actual description of the changes):
```bash
git commit -m "Automated commit of recent changes"
```
3. Push to the GitHub repository:
```bash
git push
```
